
/* libwsn 
 * since AVR Studio doesn't support build library directly, we had to create an empty
 * project and generate the library manually later based on the binary object files 
 */
int main(void)
{
    return 0;
}

/**
 * This module defines the interface of OSI network layer (NET) interface. All the 
 * NET layer implementations must abind by this interface so that the higher level
 * component can freely change their NET layer protocols.
 */
 
 broadcast
 send
 recv
 